using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;
using UnityEngine.XR.ARFoundation;
using UnityEngine.XR.ARSubsystems;

public class AnchorController1 : MonoBehaviour
{
    /*[SerializeField]
    private ARAnchorManager anchorManager;

    [SerializeField]
    private ARPlaneManager planeManager;

    private static List<hitList>

    private GameObject newAnchor;

    private ARAnchor newAnchorAR;

    public void startu()
    {
        if (TryGetTouchPosition(out Vector2 touchPosition))
        {
            return;
        }

        if (raycastManager.Raycast(touchPosition, hitlist, TrackableType))
        {
            Pose hitPose = hitList[0].pose;



            if (newAnchor != null)
            {
                newAnchor = anchorManager.gameObject.AddComponent<ARAnchor>();

                if (newAnchor != null)
                {
                    newAnchor.transform.position = hitPose.position;
                    newAnchor.transform.rotation = hitPose.rotation;


                    Instantiate(anchorPrefab, newAnchor.transform.position, newAnchor.transform.rotation);
                }
            }
        }
    }

    */    
}



